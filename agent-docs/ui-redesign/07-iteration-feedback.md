# Iteration Feedback Ledger

Short-form record of user feedback across iterations. Two sections only — no prose.

The agent **must read "Avoid" before starting any iteration.** Treat Avoid entries as hard constraints alongside `01-design-language.md`.

Entry format: `date — rule. Why: one-line reason.`

---

## Liked — candidates for promotion to design language

*(populated after first iteration review)*

---

## Avoid — do not repeat

*(populated after first iteration review)*
